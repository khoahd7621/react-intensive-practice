import { MiniNav, Navbar } from "@/components/commons";

const StoreLayout = () => {
  return (
    <div className="goto-here">
      <MiniNav />
      <Navbar />
    </div>
  );
};

export default StoreLayout;
